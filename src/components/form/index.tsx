import { Input } from "./Input";
import Container from "./container";
import Box from "./box";
import { VacationInputs } from "./vacationInput";

const Form = {
	Container: Container,
	Input: Input,
	Box: Box,
	VacationInputs: VacationInputs,
};

export default Form;
